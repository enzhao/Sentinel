# General / OS
.DS_Store
Thumbs.db
*.log

# IDEs / Editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Node.js / Dependencies (for Frontend)
# Never commit node_modules. This directory contains all third-party libraries
# and can be massive. It should be installed locally by running `npm install`.
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build output (for Frontend)
# This is the compiled code that gets deployed. We don't need to track it in git.
dist/
.output/
.nuxt/

# Frontend Test Artifacts
frontend/test-results/
frontend/playwright-report/

# Environment variables (IMPORTANT)
# This file contains secrets like API keys and database credentials.
# It should NEVER be committed to version control for security reasons.
.env
.env.*

# --- EXCEPTIONS ---
# Allow example and test .env files as they contain no secrets.
# This is the ONLY exception. All other .env files should be ignored.
!.env.example
!backend/.env.example
!backend/.env.dev
!backend/.env.test
!frontend/.env.example
!frontend/.env.development  
!frontend/.env.test

# TypeScript
# Ignore compiled TypeScript files if they are not in a build directory
*.js.map
*.tsbuildinfo

# Python
# ---------------------------------
# Virtual Environments
# Never commit virtual environments.
venv/
.venv/
env/
.env/
venv_*/

# Compiled Python files
__pycache__/
*.pyc
*.pyo
*.pyd

# Distribution / packaging
build/
dist/
*.egg-info/
backend/requirements.txt
backend/requirements-dev.txt

# Test artifacts
.pytest_cache/
.tox/
htmlcov/
.coverage

# NOTE: The .python-version file created by pyenv should NOT be ignored.
# It ensures that anyone working on the project uses the correct Python version.

# Firebase
# ---------------------------------
# Ignore Firebase debug logs
firebase-debug.log*

# Service account keys
**/serviceAccountKey.json
**/serviceAccountKey.json.local-prod


util/create_user.py
