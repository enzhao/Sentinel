# .env.test
# This file is loaded by Vitest to configure environment variables for tests.
# It ensures that tests run in a consistent environment, connecting to Firebase emulators.

# --- Firebase Emulator Configuration ---
VITE_USE_EMULATORS=true
VITE_FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
VITE_FIRESTORE_EMULATOR_HOST=localhost:8080

# --- Firebase Project Configuration (Dummy values for tests) ---
# These values are required for the Firebase SDK to initialize, but they are
# not used when connecting to emulators.
VITE_FIREBASE_API_KEY="test-api-key"
VITE_FIREBASE_AUTH_DOMAIN="test-auth-domain"
VITE_FIREBASE_PROJECT_ID="sentinel-invest"
VITE_FIREBASE_STORAGE_BUCKET="test-storage-bucket"
VITE_FIREBASE_MESSAGING_SENDER_ID="test-sender-id"
VITE_FIREBASE_APP_ID="test-app-id"
VITE_FIREBASE_MEASUREMENT_ID="test-measurement-id"